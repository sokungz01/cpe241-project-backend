version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 5500:5500
    env_file:
      - .env
    depends_on:
      - mysql
    restart: on-failure
